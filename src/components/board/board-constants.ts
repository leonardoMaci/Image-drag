/** Number of images per row. The board is a fixed grid — never a free canvas. */
export const GRID_COLUMNS = 5;

/** Gap between grid cells, in px. */
export const GRID_GAP = 16;

/** Aspect ratio of an image cell (width / height). */
export const CELL_ASPECT_RATIO = 4 / 3;

/** Max characters shown in the cell caption before truncation. */
export const CAPTION_MAX_CHARS = 40;
